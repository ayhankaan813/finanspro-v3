---
phase: 06-borc-alacak-yonetim-sayfasi
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - apps/frontend/src/hooks/use-api.ts
  - apps/frontend/src/app/(dashboard)/borclar/page.tsx
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03]

must_haves:
  truths:
    - "Summary cards show total borc, total alacak, net durum, and active debt count aggregated across ALL entity types"
    - "Create debt dialog has entity type selector (Finansor/Partner/Dis Kisi/Organizasyon) for both lender and borrower, with entity dropdown populated after type selection"
    - "Open debts tab shows entity type badges (colored) next to lender/borrower names"
    - "Payment dialog allows partial/full payment against open debts"
    - "Cancel dialog only available for debts with zero payments"
    - "Islem gecmisi tab shows all debt creation and payment events in chronological order"
  artifacts:
    - path: "apps/frontend/src/hooks/use-api.ts"
      provides: "Updated Debt interfaces with entity types, renamed hooks"
      exports: ["useDebtSummary", "useDebts", "useDebtEntitySummary", "useDebtMatrix", "useCreateDebt"]
    - path: "apps/frontend/src/app/(dashboard)/borclar/page.tsx"
      provides: "Complete borclar page with summary, open debts, history tabs"
      min_lines: 500
  key_links:
    - from: "apps/frontend/src/app/(dashboard)/borclar/page.tsx"
      to: "/api/debts/*"
      via: "React Query hooks from use-api.ts"
      pattern: "useDebt(Summary|s|Matrix|EntitySummary)"
    - from: "apps/frontend/src/hooks/use-api.ts"
      to: "/api/debts/entity-summary"
      via: "API client fetch"
      pattern: "api\\.get.*entity-summary"
---

<objective>
Rewrite the frontend hooks and the /borclar page to support the new polymorphic entity debt system. This plan covers summary cards (PAGE-01), open debts list with entity type badges and dialogs (PAGE-02), and transaction history tab (PAGE-03).

Purpose: The existing frontend was financier-only. The user needs entity type selectors in the create dialog, entity type badges in tables, and summary data aggregated across all entity types.

Output: Updated use-api.ts interfaces/hooks + complete rewrite of borclar/page.tsx for 3 of 4 tabs (matrix tab in plan 03).
</objective>

<execution_context>
@/Users/emreyilmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emreyilmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-borc-alacak-yonetim-sayfasi/06-CONTEXT.md
@.planning/phases/06-borc-alacak-yonetim-sayfasi/06-01-SUMMARY.md

@apps/frontend/src/hooks/use-api.ts (lines 2268-2456 — debt section)
@apps/frontend/src/app/(dashboard)/borclar/page.tsx

<interfaces>
<!-- Backend API contracts AFTER Plan 06-01 -->

POST /api/debts
  Body: { lender_type: DebtEntityType, lender_id: string, borrower_type: DebtEntityType, borrower_id: string, amount: string, description?: string }
  Response: { success: true, data: Debt }

GET /api/debts?status=ACTIVE&entity_id=xxx&entity_type=FINANCIER&page=1&limit=20
  Response: { success: true, data: { items: Debt[], pagination: {...} } }

GET /api/debts/summary
  Response: { success: true, data: DebtSummary }

GET /api/debts/entity-summary  (RENAMED from /financier-summary)
  Response: { success: true, data: DebtEntitySummary[] }
  DebtEntitySummary = { entity: { id, name, code?, type }, total_receivable, total_owed, net_position }

GET /api/debts/matrix
  Response: { success: true, data: { entities: EntityInfo[], matrix: MatrixEntry[] } }

DebtEntityType = 'FINANCIER' | 'PARTNER' | 'EXTERNAL_PARTY' | 'ORGANIZATION'

Debt = {
  id, status, lender_type, lender_id, borrower_type, borrower_id,
  amount, remaining_amount, description, cancelled_at, cancellation_reason,
  created_by, created_at, updated_at,
  lender: { id, name, code?, type },
  borrower: { id, name, code?, type },
  payments?: DebtPayment[]
}

<!-- Existing entity list hooks (already in use-api.ts) -->
useFinanciers(params?) → PaginatedResponse<Financier>  (line 362)
usePartners(params?) → PaginatedResponse<Partner>  (line 311)
useExternalParties(params?) → PaginatedResponse<ExternalParty>  (line 572)

<!-- Entity type badge colors from CONTEXT.md -->
Finansor = #2a6f97 (blue)
Partner = #059669 (green)
DisKisi = #d97706 (orange)
Org = #7c3aed (purple)

<!-- Header gradient style (from MEMORY.md) -->
from-slate-900 to-slate-800
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update use-api.ts Debt interfaces and hooks</name>
  <files>apps/frontend/src/hooks/use-api.ts</files>
  <action>
  Replace the entire DEBT section (lines ~2268-2456) with updated interfaces and hooks:

  **A. Update interfaces:**

  1. `DebtEntityType` — new type alias:
     ```typescript
     export type DebtEntityType = 'FINANCIER' | 'PARTNER' | 'EXTERNAL_PARTY' | 'ORGANIZATION';
     ```

  2. `DebtEntityInfo` — entity reference with type:
     ```typescript
     export interface DebtEntityInfo {
       id: string;
       name: string;
       code?: string;
       type: DebtEntityType;
     }
     ```

  3. Update `Debt` interface:
     - Add `lender_type: DebtEntityType` and `borrower_type: DebtEntityType`
     - Change `lender` and `borrower` types from `{ id: string; name: string; code: string }` to `DebtEntityInfo`

  4. `DebtPayment` — keep as-is

  5. `DebtSummary` — keep as-is (backend response unchanged)

  6. Replace `DebtFinancierSummary` with `DebtEntitySummary`:
     ```typescript
     export interface DebtEntitySummary {
       entity: DebtEntityInfo;
       total_receivable: number;
       total_owed: number;
       net_position: number;
     }
     ```

  7. Update `DebtMatrixResponse`:
     ```typescript
     export interface DebtMatrixResponse {
       entities: DebtEntityInfo[];
       matrix: Array<{
         lender: DebtEntityInfo;
         borrower: DebtEntityInfo;
         amount: number;
       }>;
     }
     ```

  8. `DebtListResponse` — keep as-is (items type auto-updated via Debt change)

  **B. Update hooks:**

  1. `useDebtSummary()` — keep as-is (endpoint unchanged)
  2. `useDebts(params?)` — update params type: replace `financier_id` with `entity_id?: string; entity_type?: DebtEntityType`; update query param construction accordingly
  3. `useDebt(id)` — keep as-is
  4. Rename `useDebtFinancierSummary()` to `useDebtEntitySummary()` — change endpoint to `/api/debts/entity-summary`, update query key to `["debt-entity-summary"]`
  5. `useDebtMatrix()` — keep as-is (endpoint unchanged, response type auto-updated)

  **C. Update mutations:**

  1. `useCreateDebt()` — update mutationFn input type to include `lender_type`, `borrower_type`, `lender_id`, `borrower_id`, `amount`, `description?`. Update invalidation keys to include `["debt-entity-summary"]` instead of `["debt-financier-summary"]`.
  2. `usePayDebt()` — update invalidation keys (replace financier-summary with entity-summary)
  3. `useCancelDebt()` — update invalidation keys (replace financier-summary with entity-summary)

  IMPORTANT: Keep the `useDebtFinancierSummary` export temporarily as an alias for backward compat (it can be removed later):
  ```typescript
  /** @deprecated Use useDebtEntitySummary instead */
  export const useDebtFinancierSummary = useDebtEntitySummary;
  ```
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - DebtEntityType type exported
  - Debt interface includes lender_type/borrower_type and DebtEntityInfo objects
  - useDebtEntitySummary() hook hits /api/debts/entity-summary
  - useCreateDebt() accepts entity type fields
  - useDebts() accepts entity_id/entity_type filter params
  - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite borclar/page.tsx — summary cards, open debts tab, history tab, create/payment/cancel dialogs</name>
  <files>apps/frontend/src/app/(dashboard)/borclar/page.tsx</files>
  <action>
  Complete rewrite of the page. The page structure is: header with "Yeni Borc" button, 4 summary cards, and tabbed content (Acik Borclar, Islem Gecmisi, Entity Matrix).

  **CRITICAL: Fix React key warning from UAT** — use `<Fragment key={...}>` not bare `<>` in TableBody map.

  **A. Imports and entity type helpers:**

  Import from use-api.ts:
  - `useDebtSummary, useDebtEntitySummary, useDebts, useDebt, useCreateDebt, usePayDebt, useCancelDebt, useDebtMatrix`
  - `useFinanciers, usePartners, useExternalParties`
  - Types: `Debt, DebtEntityType, DebtEntityInfo, DebtEntitySummary`

  Create helper constants:
  ```typescript
  const ENTITY_TYPE_LABELS: Record<DebtEntityType, string> = {
    FINANCIER: 'Finansor',
    PARTNER: 'Partner',
    EXTERNAL_PARTY: 'Dis Kisi',
    ORGANIZATION: 'Organizasyon',
  };

  const ENTITY_TYPE_COLORS: Record<DebtEntityType, string> = {
    FINANCIER: 'bg-[#2a6f97]/10 text-[#2a6f97] border-[#2a6f97]/20',
    PARTNER: 'bg-emerald-50 text-emerald-700 border-emerald-200',
    EXTERNAL_PARTY: 'bg-amber-50 text-amber-700 border-amber-200',
    ORGANIZATION: 'bg-violet-50 text-violet-700 border-violet-200',
  };
  ```

  Create `EntityBadge` component:
  ```typescript
  function EntityBadge({ type, name }: { type: DebtEntityType; name: string }) {
    return (
      <div className="flex items-center gap-2">
        <Badge variant="outline" className={`text-xs ${ENTITY_TYPE_COLORS[type]}`}>
          {ENTITY_TYPE_LABELS[type]}
        </Badge>
        <span className="font-medium">{name}</span>
      </div>
    );
  }
  ```

  **B. Page header:**
  - Gradient background: `from-slate-900 to-slate-800` (matching existing report pages)
  - Title: "Borc / Alacak Yonetimi" with HandCoins icon
  - "Yeni Borc" button (Plus icon) that opens create dialog

  **C. Summary cards (PAGE-01):**
  4 cards in responsive grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`):
  1. Toplam Borc (TrendingDown icon, red accent) — sum from `useDebtEntitySummary`: sum of all `total_owed` values
  2. Toplam Alacak (TrendingUp icon, green accent) — sum of all `total_receivable` values
  3. Net Durum (Scale icon, conditional green/red) — total alacak minus total borc; green if positive, red if negative
  4. Aktif Borc (Activity icon, amber accent) — `useDebtSummary().active_debt_count`

  All amounts in `formatMoney()` with mono font (`font-mono`).

  **D. Tabs:**
  3 tabs: "Acik Borclar" (List icon), "Islem Gecmisi" (Clock icon), "Entity Matrix" (Grid3X3 icon).
  The Matrix tab content will be a PLACEHOLDER for now (just text "Matrix yuklenecek...") — Plan 06-03 fills it.

  **E. Acik Borclar tab (PAGE-02):**
  - Filter bar: entity type select (all/Finansor/Partner/Dis Kisi/Org) + status select (Aktif/Odenmis/Iptal)
  - Table columns: Borc Veren (EntityBadge), Borc Alan (EntityBadge), Baslangic Tutari, Kalan Bakiye, Ilerleme (progress bar div), Tarih, Durum (badge)
  - Use `useDebts({ status: filterStatus, entity_type: filterEntityType, page, limit: 20 })`
  - Expandable rows: clicking a row shows payment history + action buttons
  - Action buttons in expanded area:
    - "Odeme Yap" (CreditCard icon) — opens payment dialog
    - "Iptal Et" (XCircle icon) — only shown when debt has zero payments; opens cancel dialog
  - Pagination at bottom

  **F. Islem Gecmisi tab (PAGE-03):**
  - Fetch ALL debts (no status filter, large limit like 100) to show chronological history
  - Show a unified timeline table:
    - Each debt creation as a "Borc Olusturuldu" row
    - Each payment as an "Odeme Yapildi" row
  - Flatten debts + their payments into a single sorted array by date (most recent first)
  - Columns: Tarih, Islem Tipi (badge), Taraflar (EntityBadge for lender → borrower), Tutar, Aciklama

  **G. Create Debt Dialog:**
  - Two-step entity selection for both lender and borrower:
    1. Entity type dropdown (Finansor/Partner/Dis Kisi/Organizasyon)
    2. Entity ID dropdown — populated LAZILY based on selected type:
       - FINANCIER: use `useFinanciers({ limit: 100, enabled: selectedType === 'FINANCIER' })`
       - PARTNER: use `usePartners({ limit: 100, enabled: selectedType === 'PARTNER' })`
       - EXTERNAL_PARTY: use `useExternalParties({ limit: 100, enabled: selectedType === 'EXTERNAL_PARTY' })`
       - ORGANIZATION: auto-select `org-main-account` (single option, no dropdown needed)
    3. If same type + same entity selected for both → show error "Ayni kisi hem borc veren hem borc alan olamaz"
  - Amount field (number input)
  - Description field (optional textarea)
  - Submit calls `useCreateDebt().mutateAsync()`
  - On success: toast + close dialog

  **H. Payment Dialog:**
  - Shows current debt info (lender → borrower, remaining amount)
  - Amount input with "Tamami Ode" button that fills remaining
  - Description optional textarea
  - Submit calls `usePayDebt().mutateAsync({ debtId, amount, description })`
  - On success: toast + close dialog

  **I. Cancel Dialog:**
  - Warning alert with AlertTriangle icon
  - Shows debt info
  - Optional cancellation reason textarea
  - Confirm button (red)
  - Submit calls `useCancelDebt().mutateAsync({ debtId, cancellation_reason })`
  - On success: toast + close dialog

  **J. Loading states:**
  - Summary cards: skeleton animation while loading
  - Tables: skeleton rows while loading

  **DESIGN RULES:**
  - Card-based layout with `rounded-3xl` or `rounded-2xl`
  - Shadows: `shadow-lg`, `shadow-xl`
  - Transitions: `transition-all duration-300`
  - Numbers: `font-mono` for financial amounts
  - Turkish labels in UI, English in code
  - Responsive: mobile-first grid
  </action>
  <verify>
    <automated>cd apps/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - Page renders 4 summary cards with correct data from useDebtEntitySummary + useDebtSummary
  - Create dialog has two-step entity type + entity ID selection for lender/borrower
  - Acik Borclar tab shows entity badges, progress bars, expandable rows
  - Islem Gecmisi tab shows flattened chronological timeline of debts + payments
  - Matrix tab has placeholder content
  - Payment and cancel dialogs work correctly
  - No React key warnings (Fragment has key prop)
  - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. `cd apps/frontend && npx tsc --noEmit` — TypeScript compiles
2. Start both backend and frontend
3. Navigate to /borclar — page loads with summary cards
4. Click "Yeni Borc" — dialog shows entity type selectors
5. Select Partner → Dis Kisi → enter amount → submit
6. Verify new debt appears in Acik Borclar list with entity badges
7. Click debt row → expandable area shows with payment/cancel buttons
8. Check Islem Gecmisi tab — shows chronological events
9. Check browser console — no React key warnings
</verification>

<success_criteria>
- Summary cards aggregate data across all entity types (PAGE-01)
- Open debts list shows entity type badges and supports entity type filtering (PAGE-02)
- Transaction history shows unified chronological timeline (PAGE-03)
- Create dialog supports all 4 entity types with lazy-loaded entity dropdowns
- No React key warnings in console
</success_criteria>

<output>
After completion, create `.planning/phases/06-borc-alacak-yonetim-sayfasi/06-02-SUMMARY.md`
</output>
