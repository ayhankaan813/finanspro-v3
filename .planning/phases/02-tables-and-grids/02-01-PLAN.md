---
phase: 02-tables-and-grids
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx
  - apps/frontend/src/app/(dashboard)/organization/personnel/page.tsx
autonomous: true
requirements:
  - TABL-01
  - TABL-02
  - TABL-03
must_haves:
  truths:
    - "At 375px viewport width, the financier detail page table can be scrolled horizontally to reveal all 9 columns without any column being clipped"
    - "The financier detail table sticky header and sticky footer remain pinned while scrolling vertically"
    - "The personnel desktop table (visible at sm: and above) has a horizontal scroll wrapper for tablet-width safety"
    - "Tables that already have overflow-x-auto wrappers (daily, monthly, kasa-raporu, reconciliation, external-parties detail) continue to work — no regressions"
  artifacts:
    - path: "apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx"
      provides: "9-column financier detail table with horizontal scroll"
      contains: "overflow-x-auto"
    - path: "apps/frontend/src/app/(dashboard)/organization/personnel/page.tsx"
      provides: "Personnel desktop table with horizontal scroll wrapper"
      contains: "overflow-x-auto"
  key_links:
    - from: "apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx"
      to: "Card > CardContent > scroll containers"
      via: "overflow-x-auto wrapper inside overflow-y-auto container; table-fixed removed for scroll to engage"
      pattern: "overflow-x-auto"
    - from: "apps/frontend/src/app/(dashboard)/organization/personnel/page.tsx"
      to: "hidden sm:block desktop table container"
      via: "overflow-x-auto class added to the desktop table wrapper div"
      pattern: "overflow-x-auto"
---

<objective>
Add horizontal scroll wrappers to the two tables that are missing them: the financier detail 9-column table and the personnel desktop table. Verify (but do not modify) the tables that already have overflow-x-auto.

Purpose: Without horizontal scroll, the financier detail table's 9 columns are crushed at 375px because table-fixed forces equal column distribution. The personnel desktop table is only shown at sm+ but still needs a scroll wrapper for safety at tablet widths (640-768px). All other tables either already have scroll wrappers or are approved designs that should not be touched.

Output: financiers/[id]/page.tsx gains overflow-x-auto wrapper and loses table-fixed; personnel/page.tsx desktop table gains overflow-x-auto. No changes to already-working tables.
</objective>

<execution_context>
@/Users/emreyilmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emreyilmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/PROJECT.md
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/ROADMAP.md
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/STATE.md

<interfaces>
<!-- Current state of files being modified. Executor uses these directly. -->

From apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx (lines 302-327):
```tsx
{/* Data Table */}
<Card className="border-0 shadow-lg">
  <div className="border-b border-twilight-100 bg-twilight-50/30 px-3 sm:px-6 py-2 sm:py-4">
    <h2 className="text-xs sm:text-lg font-bold text-twilight-900">
      {selectedMonth
        ? `${MONTHS[selectedMonth - 1]} ${selectedYear} Günlük Detay`
        : `${selectedYear} Yıllık Detay`
      }
    </h2>
  </div>
  <CardContent className="p-0">
    <div className="max-h-[65vh] overflow-y-auto">
      <table className="w-full table-fixed">
        <thead className="sticky top-0 z-10 bg-twilight-50/95 backdrop-blur-sm shadow-[0_1px_0_0_theme(colors.twilight.100)]">
          <tr>
            <th className="w-[28px] sm:w-[70px] px-1 sm:px-3 py-1.5 sm:py-3 text-left text-[9px] sm:text-xs font-semibold text-twilight-600 uppercase">TARİH</th>
            <th className="px-0.5 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-emerald-600 uppercase">YAT.</th>
            <th className="px-0.5 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-rose-600 uppercase">ÇEKİM</th>
            <th className="px-0.5 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-orange-600 uppercase">ÖDEME</th>
            <th className="px-0.5 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-blue-600 uppercase">TAK.</th>
            <th className="px-0.5 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-purple-600 uppercase">TES.</th>
            <th className="px-0.5 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-indigo-600 uppercase">T.KOM.</th>
            <th className="px-0.5 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-slate-600 uppercase">KOM.</th>
            <th className="px-1 sm:px-3 py-1.5 sm:py-3 text-right text-[9px] sm:text-xs font-semibold text-twilight-900 uppercase">BAKİYE</th>
          </tr>
        </thead>
        <!-- tbody and tfoot follow -->
      </table>
    </div>
  </CardContent>
</Card>
```

IMPORTANT context: The sites/[id] and partners/[id] detail tables use a SIMILAR structure (table-fixed with tiny mobile text) and are APPROVED AS-IS. Do NOT touch them. Only financiers/[id] needs the scroll fix because it has 9 columns (vs 5-8 for the others) which causes clipping.

From apps/frontend/src/app/(dashboard)/organization/personnel/page.tsx (lines 520-557):
```tsx
<div className="bg-white rounded-xl ring-1 ring-slate-100 overflow-hidden">
  {/* Desktop Table */}
  <div className="hidden sm:block">
    <table className="w-full text-sm">
      <thead>
        <tr className="border-b border-slate-100 text-xs text-slate-500">
          <th className="text-left p-3 font-medium">Tarih</th>
          <th className="text-left p-3 font-medium">Tur</th>
          <th className="text-left p-3 font-medium">Donem</th>
          <th className="text-right p-3 font-medium">Tutar</th>
          <th className="text-left p-3 font-medium">Aciklama</th>
        </tr>
      </thead>
      <tbody className="divide-y divide-slate-50">
        {/* payment rows */}
      </tbody>
    </table>
  </div>

  {/* Mobile Cards */}
  <div className="sm:hidden divide-y divide-slate-100">
    {/* mobile card view */}
  </div>
</div>
```

NOTE: This personnel table already has a mobile card fallback (sm:hidden). The desktop table (hidden sm:block) only needs overflow-x-auto for tablet safety. Do NOT add whitespace-nowrap — the 5 columns are compact and the description column uses truncate already.

Tables to VERIFY ONLY (already have overflow-x-auto — do not modify):
- reports/daily/page.tsx — has overflow-x-auto
- reports/monthly/page.tsx — has overflow-x-auto
- reports/kasa-raporu/page.tsx — has overflow-x-auto + sticky first column
- reports/reconciliation/page.tsx — has overflow-x-auto
- external-parties/[id]/page.tsx — has overflow-x-auto

Tables to LEAVE ALONE (approved designs):
- sites/[id]/page.tsx — approved table-fixed with tiny mobile text
- partners/[id]/page.tsx — approved table-fixed with tiny mobile text

Tables DEFERRED to Phase 4:
- transactions/page.tsx — TXNP-01 scope, uses column-hiding strategy
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add horizontal scroll wrapper to financier detail table</name>
  <files>apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx</files>
  <action>
    Open apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx. Find the table structure starting at line 312-314:

    ```tsx
    <CardContent className="p-0">
      <div className="max-h-[65vh] overflow-y-auto">
        <table className="w-full table-fixed">
    ```

    Make these changes:

    1. On the `<table>` element (line 314), change `table-fixed` to `min-w-[560px]`:
       ```tsx
       <table className="w-full min-w-[560px]">
       ```
       WHY: `table-fixed` forces equal column distribution which crushes 9 columns at 375px. Removing it and adding `min-w-[560px]` lets columns size naturally and ensures the scroll container has something to scroll.

    2. Wrap the table in an `overflow-x-auto` div. The existing `overflow-y-auto` div stays, but the new wrapper goes INSIDE it, around the `<table>`:
       ```tsx
       <CardContent className="p-0">
         <div className="max-h-[65vh] overflow-y-auto">
           <div className="overflow-x-auto">
             <table className="w-full min-w-[560px]">
               <!-- thead, tbody, tfoot unchanged -->
             </table>
           </div>
         </div>
       </CardContent>
       ```

    3. Do NOT add `-mx-3 sm:mx-0` here because the table is inside a Card with `p-0` on CardContent — there is no padding to offset.

    4. Do NOT add `whitespace-nowrap` to cells — they already use `text-[9px]` with compact padding (`px-0.5`) and financial numbers are short enough.

    5. Do NOT touch the thead sticky positioning (`sticky top-0 z-10`) — it will still work because the vertical scroll is on the outer container.

    6. Do NOT touch the tfoot sticky positioning (`sticky bottom-0 z-10`).

    7. Close the new `</div>` for `overflow-x-auto` AFTER the closing `</table>` tag and BEFORE the closing `</div>` of the `overflow-y-auto` container.
  </action>
  <verify>
    <automated>grep -n "overflow-x-auto" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx" && grep -n "min-w-\[560px\]" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx" && ! grep -n "table-fixed" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/app/(dashboard)/financiers/[id]/page.tsx"</automated>
  </verify>
  <done>financiers/[id]/page.tsx table has overflow-x-auto wrapper, min-w-[560px] instead of table-fixed, and no table-fixed class remaining in the file</done>
</task>

<task type="auto">
  <name>Task 2: Add horizontal scroll wrapper to personnel desktop table</name>
  <files>apps/frontend/src/app/(dashboard)/organization/personnel/page.tsx</files>
  <action>
    Open apps/frontend/src/app/(dashboard)/organization/personnel/page.tsx. Find the desktop table container at line 520-522:

    ```tsx
    <div className="bg-white rounded-xl ring-1 ring-slate-100 overflow-hidden">
      {/* Desktop Table */}
      <div className="hidden sm:block">
        <table className="w-full text-sm">
    ```

    Make this change:

    1. Add `overflow-x-auto` to the `hidden sm:block` div:
       ```tsx
       <div className="hidden sm:block overflow-x-auto">
       ```

    That is the ONLY change. Do NOT:
    - Touch the mobile cards section (`sm:hidden` div)
    - Add `whitespace-nowrap` to cells (the 5-column table is compact enough)
    - Add `-mx-3 sm:mx-0` (table is inside a rounded container, negative margin would break rounded corners)
    - Change the parent `overflow-hidden` to something else (it is used for the rounded corners on the container, and the inner `overflow-x-auto` takes precedence for the table scroll)
    - Add `min-w-[Xpx]` to the table (5 columns fit at sm: widths without forcing a minimum)
  </action>
  <verify>
    <automated>grep -n "hidden sm:block overflow-x-auto" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/app/(dashboard)/organization/personnel/page.tsx"</automated>
  </verify>
  <done>The personnel desktop table wrapper div has both `hidden sm:block` and `overflow-x-auto` classes</done>
</task>

</tasks>

<verification>
After both tasks complete, verify:

1. financiers/[id]/page.tsx:
   - Contains `overflow-x-auto` wrapper around the table
   - Contains `min-w-[560px]` on the table element
   - Does NOT contain `table-fixed`
   - Sticky thead (`sticky top-0`) and tfoot (`sticky bottom-0`) classes are preserved

2. organization/personnel/page.tsx:
   - The desktop table div has `overflow-x-auto` added
   - The mobile cards div (`sm:hidden`) is unchanged
   - No other changes in the file

3. Files NOT modified (verify they still work — no regressions from Phase 1):
   - reports/daily/page.tsx — still has overflow-x-auto
   - reports/monthly/page.tsx — still has overflow-x-auto
   - reports/kasa-raporu/page.tsx — still has overflow-x-auto
   - sites/[id]/page.tsx — still has table-fixed (approved)
   - partners/[id]/page.tsx — still has table-fixed (approved)
</verification>

<success_criteria>
- financiers/[id]/page.tsx: table scrolls horizontally at 375px, all 9 columns accessible via scroll
- financiers/[id]/page.tsx: sticky header and footer continue to pin during vertical scroll
- organization/personnel/page.tsx: desktop table has overflow-x-auto for tablet safety
- No changes to any other table files (sites, partners, reports, external-parties, transactions)
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create /Users/emreyilmaz/Desktop/finanspro v3/.planning/phases/02-tables-and-grids/02-01-SUMMARY.md
</output>
