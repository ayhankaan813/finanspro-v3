---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/ui/input.tsx
  - apps/frontend/src/components/ui/select.tsx
  - apps/frontend/src/components/ui/textarea.tsx
  - apps/frontend/src/components/ui/button.tsx
  - apps/frontend/src/components/ui/tabs.tsx
  - apps/frontend/src/styles/globals.css
autonomous: true
requirements:
  - GLOB-02
  - GLOB-03
  - GLOB-05
must_haves:
  truths:
    - "Tapping any input, select, or textarea on iOS Safari does not trigger page zoom"
    - "All buttons (size: default and icon) are at least 44px tall and tappable without precision on touch devices"
    - "TabsTrigger elements meet 44px minimum touch target height"
    - "Financial amounts (formatMoney output such as 1.234.567,89 TL) never break mid-number across two lines"
  artifacts:
    - path: "apps/frontend/src/components/ui/input.tsx"
      provides: "Input with text-base sm:text-sm (16px on mobile, 14px on desktop)"
      contains: "text-base sm:text-sm"
    - path: "apps/frontend/src/components/ui/select.tsx"
      provides: "SelectTrigger with text-base sm:text-sm"
      contains: "text-base sm:text-sm"
    - path: "apps/frontend/src/components/ui/textarea.tsx"
      provides: "Textarea with text-base sm:text-sm"
      contains: "text-base sm:text-sm"
    - path: "apps/frontend/src/components/ui/button.tsx"
      provides: "Button sizes at 44px touch minimum"
      contains: "h-11"
    - path: "apps/frontend/src/components/ui/tabs.tsx"
      provides: "TabsTrigger with min-h-[44px]"
      contains: "min-h-[44px]"
    - path: "apps/frontend/src/styles/globals.css"
      provides: ".font-amount with white-space: nowrap"
      contains: "white-space: nowrap"
  key_links:
    - from: "apps/frontend/src/styles/globals.css"
      to: "All elements using .font-amount class"
      via: "CSS class cascades to every formatMoney() output wrapped in .font-amount"
      pattern: "font-amount"
    - from: "apps/frontend/src/components/ui/input.tsx"
      to: "All form inputs across 20+ pages"
      via: "Base Input component used everywhere via shadcn/ui"
      pattern: "text-base sm:text-sm"
---

<objective>
Apply three global mobile fixes to shared UI primitives: (1) 16px font-size on all input components to prevent iOS Safari zoom, (2) 44px minimum touch targets on buttons and tabs, and (3) white-space: nowrap on .font-amount to protect financial numbers from mid-number line breaks.

Purpose: These are base component changes. Every form and button across all 20+ pages inherits these fixes instantly without per-page work.
Output: input.tsx, select.tsx, textarea.tsx get text-base sm:text-sm; button.tsx gets h-11 default/icon sizes; tabs.tsx gets min-h-[44px] on TabsTrigger; globals.css .font-amount gets white-space: nowrap.
</objective>

<execution_context>
@/Users/emreyilmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emreyilmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/PROJECT.md
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/ROADMAP.md
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/STATE.md

<interfaces>
<!-- Exact current state of all files being modified. -->

From apps/frontend/src/components/ui/input.tsx (line 13):
```tsx
// CURRENT — text-sm triggers iOS zoom
"flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"

// REQUIRED — text-base on mobile (16px), sm:text-sm on desktop (14px, preserves visual)
"flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-base sm:text-sm ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"
```

From apps/frontend/src/components/ui/select.tsx SelectTrigger (line 21):
```tsx
// CURRENT — text-sm triggers iOS zoom on select focus
"flex h-10 w-full items-center justify-between rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 [&>span]:line-clamp-1"

// REQUIRED
"flex h-10 w-full items-center justify-between rounded-md border border-input bg-background px-3 py-2 text-base sm:text-sm ring-offset-background placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 [&>span]:line-clamp-1"
```

From apps/frontend/src/components/ui/textarea.tsx (line 13):
```tsx
// CURRENT — text-sm triggers iOS zoom on textarea focus
"flex min-h-[80px] w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"

// REQUIRED
"flex min-h-[80px] w-full rounded-md border border-input bg-background px-3 py-2 text-base sm:text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"
```

From apps/frontend/src/components/ui/button.tsx size variants (lines 24-29):
```tsx
// CURRENT — h-10 (40px) default is 4px short of 44px minimum
size: {
  default: "h-10 px-4 py-2",
  sm: "h-9 rounded-md px-3",
  lg: "h-11 rounded-md px-8",
  icon: "h-10 w-10",
},

// REQUIRED — h-11 (44px) for default and icon; sm and lg unchanged
size: {
  default: "h-11 px-4 py-2",
  sm: "h-9 rounded-md px-3",
  lg: "h-11 rounded-md px-8",
  icon: "h-11 w-11",
},
```

From apps/frontend/src/components/ui/tabs.tsx TabsTrigger (line 32):
```tsx
// CURRENT — no explicit min-height; py-1.5 padding may render under 44px
"inline-flex items-center justify-center whitespace-nowrap rounded-sm px-3 py-1.5 text-sm font-medium ring-offset-white transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-twilight-950 focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 data-[state=active]:bg-white data-[state=active]:text-twilight-950 data-[state=active]:shadow-sm"

// REQUIRED — add min-h-[44px] after py-1.5
"inline-flex items-center justify-center whitespace-nowrap rounded-sm px-3 py-1.5 min-h-[44px] text-sm font-medium ring-offset-white transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-twilight-950 focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 data-[state=active]:bg-white data-[state=active]:text-twilight-950 data-[state=active]:shadow-sm"
```

From apps/frontend/src/styles/globals.css .font-amount (lines 104-109):
```css
/* CURRENT — no white-space: nowrap; numbers break mid-digit */
.font-amount {
  font-family: theme("fontFamily.mono");
  font-variant-numeric: tabular-nums;
  letter-spacing: -0.025em;
}

/* REQUIRED — add white-space: nowrap as 4th property */
.font-amount {
  font-family: theme("fontFamily.mono");
  font-variant-numeric: tabular-nums;
  letter-spacing: -0.025em;
  white-space: nowrap;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix iOS Safari zoom — update input.tsx, select.tsx, textarea.tsx to text-base sm:text-sm</name>
  <files>
    apps/frontend/src/components/ui/input.tsx,
    apps/frontend/src/components/ui/select.tsx,
    apps/frontend/src/components/ui/textarea.tsx
  </files>
  <action>
    Make exactly one change to each of the three files: replace `text-sm` with `text-base sm:text-sm` in the base className string of the form element. Keep all other classes unchanged.

    **input.tsx (line 13):**
    Find: `px-3 py-2 text-sm ring-offset-background`
    Replace: `px-3 py-2 text-base sm:text-sm ring-offset-background`
    Do NOT touch `file:text-sm` — that class controls file upload button text separately and stays at sm.

    **select.tsx (line 21) — SelectTrigger only:**
    Find: `px-3 py-2 text-sm ring-offset-background`
    Replace: `px-3 py-2 text-base sm:text-sm ring-offset-background`
    Do NOT change SelectLabel (py-1.5 pl-8 pr-2 text-sm font-semibold) or SelectItem (text-sm) — those are not form input triggers.

    **textarea.tsx (line 13):**
    Find: `px-3 py-2 text-sm ring-offset-background`
    Replace: `px-3 py-2 text-base sm:text-sm ring-offset-background`

    Pattern explanation: text-base = 16px (iOS zoom threshold), sm:text-sm = 14px on 640px+ screens (preserves existing desktop visual exactly). Inputs appear identical on desktop; on mobile they are 2px larger which is acceptable and prevents the disorienting viewport zoom.
  </action>
  <verify>
    <automated>grep -n "text-base sm:text-sm" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/input.tsx" && grep -n "text-base sm:text-sm" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/select.tsx" && grep -n "text-base sm:text-sm" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/textarea.tsx"</automated>
  </verify>
  <done>All three files contain text-base sm:text-sm in their primary element className; file:text-sm in input.tsx is untouched</done>
</task>

<task type="auto">
  <name>Task 2: Fix touch targets — button.tsx sizes to h-11, tabs.tsx TabsTrigger min-h-[44px], globals.css .font-amount white-space: nowrap</name>
  <files>
    apps/frontend/src/components/ui/button.tsx,
    apps/frontend/src/components/ui/tabs.tsx,
    apps/frontend/src/styles/globals.css
  </files>
  <action>
    Three targeted edits:

    **button.tsx — update size variants (lines 24-29):**
    Change ONLY the `default` and `icon` size values. Leave `sm` and `lg` unchanged.

    Find:
    ```
    default: "h-10 px-4 py-2",
    ```
    Replace:
    ```
    default: "h-11 px-4 py-2",
    ```

    Find:
    ```
    icon: "h-10 w-10",
    ```
    Replace:
    ```
    icon: "h-11 w-11",
    ```

    `sm` stays `h-9` (36px) — research notes sm should be used sparingly on mobile.
    `lg` stays `h-11` (44px) — already at minimum.

    **tabs.tsx — add min-h-[44px] to TabsTrigger (line 32):**
    In the TabsTrigger className string, add `min-h-[44px]` immediately after `py-1.5`:

    Find: `rounded-sm px-3 py-1.5 text-sm font-medium`
    Replace: `rounded-sm px-3 py-1.5 min-h-[44px] text-sm font-medium`

    Do NOT touch TabsList or TabsContent — only TabsTrigger gets this change.

    **globals.css — add white-space: nowrap to .font-amount (lines 104-109):**
    The .font-amount block currently has three properties. Add a fourth:

    Find the .font-amount block:
    ```css
    .font-amount {
      font-family: theme("fontFamily.mono");
      font-variant-numeric: tabular-nums;
      letter-spacing: -0.025em;
    }
    ```

    Replace with:
    ```css
    .font-amount {
      font-family: theme("fontFamily.mono");
      font-variant-numeric: tabular-nums;
      letter-spacing: -0.025em;
      white-space: nowrap;
    }
    ```

    NOTE on .font-amount nowrap and flex containers: The white-space: nowrap makes .font-amount elements never break. If any .font-amount element inside a flex container overflows its card, the parent needs min-w-0. However, do NOT audit all usages in this task — that is per-page work in later phases. The globals.css change is the foundation fix; individual overflow issues (if any surface in Phase 2+) will be caught in those phases.
  </action>
  <verify>
    <automated>grep -n "h-11" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/button.tsx" && grep -n "min-h-\[44px\]" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/tabs.tsx" && grep -n "white-space: nowrap" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/styles/globals.css"</automated>
  </verify>
  <done>button.tsx default="h-11 px-4 py-2" and icon="h-11 w-11"; tabs.tsx TabsTrigger has min-h-[44px]; globals.css .font-amount has white-space: nowrap</done>
</task>

</tasks>

<verification>
After both tasks complete, verify all five component changes:

```bash
# GLOB-02: iOS zoom fix on all three input types
grep "text-base sm:text-sm" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/input.tsx"
grep "text-base sm:text-sm" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/select.tsx"
grep "text-base sm:text-sm" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/textarea.tsx"

# GLOB-03: Touch targets
grep "h-11" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/button.tsx"
grep "min-h-\[44px\]" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/tabs.tsx"

# GLOB-05: Financial number protection
grep "white-space: nowrap" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/styles/globals.css"

# TypeScript — no new errors
cd "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend" && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- input.tsx: text-base sm:text-sm in Input className (file:text-sm unchanged)
- select.tsx: text-base sm:text-sm in SelectTrigger className only
- textarea.tsx: text-base sm:text-sm in Textarea className
- button.tsx: default size is h-11 (was h-10), icon size is h-11 w-11 (was h-10 w-10); sm and lg sizes unchanged
- tabs.tsx: TabsTrigger has min-h-[44px] after py-1.5
- globals.css: .font-amount has white-space: nowrap as fourth property
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create /Users/emreyilmaz/Desktop/finanspro v3/.planning/phases/01-foundation/01-03-SUMMARY.md
</output>
