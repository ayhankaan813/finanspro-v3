---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/ui/dialog.tsx
autonomous: true
requirements:
  - GLOB-04
must_haves:
  truths:
    - "A shadcn Dialog opened on a 375px screen is fully visible within the viewport — no right-edge clipping, no overflow"
    - "The dialog close button (X) is reachable and tappable at 375px"
    - "Tall dialog forms (e.g. transaction form) scroll internally rather than overflowing the viewport bottom"
    - "On iOS Safari the dialog height does not jump when the address bar hides or shows"
    - "At 640px and above the dialog renders identically to its current max-w-lg appearance"
  artifacts:
    - path: "apps/frontend/src/components/ui/dialog.tsx"
      provides: "Mobile-safe DialogContent with constrained width and scrollable height"
      contains: "max-w-[calc(100vw-32px)]"
  key_links:
    - from: "apps/frontend/src/components/ui/dialog.tsx"
      to: "All pages using Dialog (transaction forms, edit forms)"
      via: "Single shared DialogContent component — one fix benefits all dialogs"
      pattern: "max-w-\\[calc\\(100vw-32px\\)\\]"
---

<objective>
Update dialog.tsx DialogContent with mobile-safe width and height constraints so that all shadcn Dialogs across the app fit within a 375px viewport without overflowing or clipping.

Purpose: The current max-w-lg (512px) overflows a 375px screen — the dialog extends off the right edge and the close button is unreachable. This single file fix benefits all ~10+ forms across every page that uses Dialog.
Output: dialog.tsx DialogContent carries mobile-constrained width (max-w-[calc(100vw-32px)] sm:max-w-lg), capped height (max-h-[90dvh]), internal scroll (overflow-y-auto), and tighter mobile padding (p-4 sm:p-6).
</objective>

<execution_context>
@/Users/emreyilmaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emreyilmaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/PROJECT.md
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/ROADMAP.md
@/Users/emreyilmaz/Desktop/finanspro v3/.planning/STATE.md

<interfaces>
<!-- Current state of DialogContent in dialog.tsx (line 40-42): -->

```tsx
// CURRENT (overflows at 375px):
className={cn(
  "fixed left-[50%] top-[50%] z-50 grid w-full max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 border bg-background p-6 shadow-lg duration-200 data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[state=closed]:slide-out-to-left-1/2 data-[state=closed]:slide-out-to-top-[48%] data-[state=open]:slide-in-from-left-1/2 data-[state=open]:slide-in-from-top-[48%] sm:rounded-lg",
  className
)}

// REQUIRED (mobile-safe):
className={cn(
  "fixed left-[50%] top-[50%] z-50 grid w-full max-w-[calc(100vw-32px)] sm:max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 border bg-background p-4 sm:p-6 shadow-lg duration-200 max-h-[90dvh] overflow-y-auto data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[state=closed]:slide-out-to-left-1/2 data-[state=closed]:slide-out-to-top-[48%] data-[state=open]:slide-in-from-left-1/2 data-[state=open]:slide-in-from-top-[48%] sm:rounded-lg",
  className
)}
```

Key changes explained:
- `max-w-lg` → `max-w-[calc(100vw-32px)] sm:max-w-lg`: 16px margin each side on mobile, full lg width on sm+
- `p-6` → `p-4 sm:p-6`: tighter padding on mobile to maximise content space
- Added `max-h-[90dvh]`: caps dialog height; dvh (not vh) because iOS Safari vh includes hidden address bar
- Added `overflow-y-auto`: dialog scrolls internally for tall forms instead of overflowing viewport
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DialogContent className in dialog.tsx with mobile-safe constraints</name>
  <files>apps/frontend/src/components/ui/dialog.tsx</files>
  <action>
    Open apps/frontend/src/components/ui/dialog.tsx. Find the DialogContent forwardRef component (starts line 32). The className string is on line 41 inside the cn() call.

    Replace the ENTIRE className string inside cn() (first argument, before the `className` variable) with:

    ```
    "fixed left-[50%] top-[50%] z-50 grid w-full max-w-[calc(100vw-32px)] sm:max-w-lg translate-x-[-50%] translate-y-[-50%] gap-4 border bg-background p-4 sm:p-6 shadow-lg duration-200 max-h-[90dvh] overflow-y-auto data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=open]:fade-in-0 data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 data-[state=closed]:slide-out-to-left-1/2 data-[state=closed]:slide-out-to-top-[48%] data-[state=open]:slide-in-from-left-1/2 data-[state=open]:slide-in-from-top-[48%] sm:rounded-lg"
    ```

    The second argument `className` remains unchanged (this is the passthrough className prop).

    Precise changes from current string:
    1. `max-w-lg` → `max-w-[calc(100vw-32px)] sm:max-w-lg`
    2. `p-6` → `p-4 sm:p-6`
    3. After `shadow-lg duration-200` add: `max-h-[90dvh] overflow-y-auto`

    Do NOT change anything else in the file:
    - DialogOverlay, DialogHeader, DialogFooter, DialogTitle, DialogDescription — all unchanged
    - The DialogPrimitive.Close button (X button) — unchanged
    - All imports and exports — unchanged

    IMPORTANT: Use dvh not vh for the max-height. dvh (dynamic viewport height) accounts for iOS Safari's address bar behavior. vh does not.
  </action>
  <verify>
    <automated>grep -n "max-w-\[calc(100vw-32px)\]" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/dialog.tsx" && grep -n "max-h-\[90dvh\]" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/dialog.tsx" && grep -n "overflow-y-auto" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/dialog.tsx"</automated>
  </verify>
  <done>dialog.tsx DialogContent has max-w-[calc(100vw-32px)] sm:max-w-lg, max-h-[90dvh], overflow-y-auto, and p-4 sm:p-6 in its className string</done>
</task>

</tasks>

<verification>
After completion, verify:

1. All three new classes exist in dialog.tsx:
   - max-w-[calc(100vw-32px)]
   - max-h-[90dvh]
   - overflow-y-auto

2. The old max-w-lg still exists but only as sm:max-w-lg (not as bare max-w-lg):
   ```
   grep "max-w-lg" "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend/src/components/ui/dialog.tsx"
   ```
   Should return: sm:max-w-lg (with sm: prefix, not bare)

3. TypeScript build has no new errors:
   ```
   cd "/Users/emreyilmaz/Desktop/finanspro v3/apps/frontend" && npx tsc --noEmit 2>&1 | head -20
   ```
</verification>

<success_criteria>
- dialog.tsx DialogContent: max-w-[calc(100vw-32px)] sm:max-w-lg (mobile constrained, desktop restored)
- dialog.tsx DialogContent: max-h-[90dvh] overflow-y-auto (height cap with internal scroll)
- dialog.tsx DialogContent: p-4 sm:p-6 (tighter mobile padding)
- No bare max-w-lg without sm: prefix remaining in DialogContent className
- No TypeScript errors introduced
- At 375px in browser DevTools, opening any Dialog shows it fully within the viewport with the X close button visible and reachable
</success_criteria>

<output>
After completion, create /Users/emreyilmaz/Desktop/finanspro v3/.planning/phases/01-foundation/01-02-SUMMARY.md
</output>
